# Nimus Sample Project

To bundle and create dist folder for development, just run:

```
npm run dev
```

**NOTE** : The project does not provide configs to set production environment. 

To perform tests (jest) run:

```
npm run test
```